<%= simple_form_for @membership, :html => { :class => 'form', :method => 'post'} do |f| %>	
		<%= f.input :individual_id, :label => 'Add an individual:', :collection => Individual.all %>
		<%= f.input :organization_id, :as => :hidden, :input_html => { :value => @organization.id } %>
		 <%= f.button :submit %>
<% end %>